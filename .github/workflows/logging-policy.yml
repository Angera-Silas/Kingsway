name: Logging policy

on:
  push:
  pull_request:

jobs:
  logging-policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
      - run: php scripts/check_logging_policy.php
